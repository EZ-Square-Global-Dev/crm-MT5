<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- Console log format (with ANSI color) -->
        <Property name="LOG_PATTERN_CONSOLE">
            %style{[%d{HH:mm:ss.SSS}]}{cyan} %highlight{%-5level} [%style{%t}{magenta}] %style{%c{1.}}{blue} - %msg%n
        </Property>
        <!-- File log format (no ANSI) -->
        <Property name="LOG_PATTERN_FILE">
            [%d{yyyy-MM-dd HH:mm:ss.SSS}] %-5level [%t] %c{1.} - %msg%n
        </Property>
        <Property name="LOG_DIR">logs</Property>
    </Properties>

    <Appenders>
        <!-- ✅ Console appender with color -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="${LOG_PATTERN_CONSOLE}"
                    disableAnsi="false"
                    noConsoleNoAnsi="false"/>
        </Console>

        <!-- ✅ Rolling file appender (no color codes) -->
        <RollingFile name="RollingFile"
                     fileName="${LOG_DIR}/app.log"
                     filePattern="${LOG_DIR}/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN_FILE}" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Log for your application's package -->
        <Logger name="com.ez.crm" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
<!--            <AppenderRef ref="RollingFile"/>-->
        </Logger>

        <!-- Spring framework logs -->
        <Logger name="org.springframework" level="warn"/>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
<!--            <AppenderRef ref="RollingFile"/>-->
        </Root>
    </Loggers>
</Configuration>
